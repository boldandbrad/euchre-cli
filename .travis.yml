language: python
python:
  - "3.7"

install:
  - pip install -r requirements.txt

script:
  - pytest

deploy:
  provider: pages
  local_dir: docs
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  keep_history: true
  on:
    branch: main